from setuptools import setup

setup(
    name='django-passwordless-auth',
    version='2020.5.26',
    install_requires=[
        'Django',
        'setuptools',
    ],
    packages=[
        'django_passwordless_auth',
    ],
)
